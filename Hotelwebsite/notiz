name attribut
inspect network payload kann man sehen was man macht
class für class



logic: 

if($_POST) * gibt auch andere variablen

var_dump = was steht in der wariable drinnen (ist eine methode)

include vs require_once
$formData
value attibut um in felder einen Wert reinzuspeichern ODER zwischen den Tags <><>
empty und isset